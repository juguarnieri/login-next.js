import "../styles/globals.css";
			export const metadata = {
				title: "Login css",
			};
			export default function RootLayout({ children }) {
				return (
					<html>
						<body>{children}</body>
					</html>
);
		}